import React from 'react';
import styles from '../styles/footer.module.scss';

const Footer = (props) => {
    return (
        <footer>
            <p>photobook footer</p>
        </footer>
    )
}

export default Footer;